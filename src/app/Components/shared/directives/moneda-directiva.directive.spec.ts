import { MonedaDirectivaDirective } from './moneda-directiva.directive';

describe('MonedaDirectivaDirective', () => {
  it('should create an instance', () => {
    const directive = new MonedaDirectivaDirective();
    expect(directive).toBeTruthy();
  });
});
