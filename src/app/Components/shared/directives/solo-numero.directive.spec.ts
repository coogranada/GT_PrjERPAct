import { SoloNumeroDirective } from './solo-numero.directive';

describe('SoloNumeroDirective', () => {
  it('should create an instance', () => {
    const directive = new SoloNumeroDirective();
    expect(directive).toBeTruthy();
  });
});
